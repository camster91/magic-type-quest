# #165 asset-gate continuation — 25 September 2026

Status: **COMMITTED IMPLEMENTATION SCOPE; FULL-APPLICATION VERIFICATION PENDING**.
Continue PR #199 from `26c3550055c4d953beee9dafc671f48473764a79` on
`codex/nature-quest-165-performance`. This record does not close #165 or authorize merge.

## Acceptance-linked scope and reproduced failures

Epic #153 and #165 with #155/#159/#163 govern this continuation. No stack, budget,
product or learner-data decision is replaced.

1. The old first-play report excluded shared core. A controlled CLI fixture with
   2,400,000 core bytes, 7,500,000 Meadow bytes and 41 additional gzip code bytes
   exited successfully, reporting only 7,500,041 bytes. The repaired command
   reports **9,900,041** and exits with failure against the unchanged 8,000,000 limit.
   A below-limit fixture still succeeds. Core loads after Start, so it belongs
   in additional first play, not in the initial interactive-shell code count.
2. A valid `viewBox="0 0 64 64"` SVG was rejected by the old malformed expression.
   The independently testable inspector now handles root viewBox coordinates,
   whitespace, negative origins, intrinsic pixel dimensions and aspect ratio.
   It rejects invalid, ambiguous or duplicate dimensions. PNG/WebP header checks
   retain their role and safely reject truncated input. This is not image-decode QA.
3. Runtime variant selection accepted an implemented flag even with pending
   approval/licence. Selection and loadable-asset filtering now require approved
   provenance. Required unapproved assets fail before fetch; optional unapproved
   assets preserve the usable required pack and return the safe fallback result.
4. When a source is also its optimized output, its path is inspected and counted
   once. Duplicate manifest outputs still fail the existing manifest validator.

Limits are shared by both CLI commands through `scripts/v2-asset-policy.mjs`:
2.5 MB initial shell, 8 MB additional Meadow/core/code, 12 MB later pack, and
1.5 MB raster. All amounts remain decimal bytes. All responsive variants are
still counted conservatively; no later pack or production art is introduced.

## Tests actually executed

| Check | Result and scope |
| --- | --- |
| New policy tests | 42 passed: limits, combined transfer, boundary and bad-measurement cases, path aliases, actual tiny PNG/WebP fixtures and SVG/header rejection |
| New runtime approval tests | 9 passed; the same suite against the prior manifest produced 3 passes and 6 failures |
| Existing loader lifecycle regressions | All 19 passed, including concurrent fetches, stale retries, cancellation and five-cycle Blob ownership |
| Combined isolated suite | 70 passed, 0 failed; repeated five additional times with all 70 passing |
| Controlled CLI fixtures | Oversized combined transfer failed; safe transfer passed; valid SVG passed and was counted once; oversized raster and orphan files both failed |
| Isolated strict TypeScript | Loader plus changed manifest passed with strict, noUncheckedIndexedAccess and exactOptionalPropertyTypes |
| JavaScript syntax | Changed/new scripts and tests passed `node --check` |
| Source identity and patch review | Recovered originals matched Git blob hashes; continuation is limited to asset policy/selection, tests and documentation |

Toolchain here: Node 22.16.0 and available TypeScript 5.8.3. The committed tests use
Vitest. In recovery, only their runner import was replaced with `node:test` in
**temporary copies**. The TypeScript loader/manifest were transpiled; only local
module extensions and Vite's BASE_URL constant were substituted. Assertions and
loader logic were not removed. The pure policy module ran as written.

CLI checks used the real command source in an isolated fixture directory, with
only Vite/asset-data-loading imports replaced by explicit fixture providers.
They exercise command failure/report wiring, not the full project build or its
actual content inventory. Tiny raster fixtures are test data, not art approvals.
The existing performance-test fixture now explicitly declares approved provenance;
its previous assertions are unchanged. That full Vitest/jsdom file was not executed here.

## Blockers and remaining acceptance evidence

Git transport and npm-registry access still fail DNS in this environment. The
available Chromium 144 rejected an attempted local page navigation with
`ERR_BLOCKED_BY_ADMINISTRATOR` under its managed URL policy. No policy was modified
or bypassed. The browser probe did not execute its page checks.

**No current-head full typecheck, lint, full Vitest, curriculum/content/theme,
production asset inventory, Vite budget build, Playwright, axe, persistence, v1,
local/cloud-boundary or production-build pass is claimed.** No FPS, input latency,
actual Phaser texture/timer/listener, screenshot, physical-device, language-review,
learner or consent evidence is created by these isolated tests.

Next engineering action: use the exact updated PR head with the locked toolchain
and run the full matrix in [the previous continuation record](165-loader-recovery-2026-09-25.md),
including `npm run validate:performance`, `npm run test:e2e:v2`, `npm run test:e2e`,
and `npm run validate:persistence`. Run the new suites under canonical Vitest:

```sh
npx vitest run test/v2-asset-policy.test.js test/v2-asset-approval.test.js test/v2-pack-loader-lifecycle.test.js test/v2-performance.test.js
```

Actual Phaser/browser checks must still exercise required image decode under CSP,
asset failure/retry, five enter/exit cycles and final shell disposal, reduced
motion, throttled feedback/FPS, save/reload and v1 coexistence. The current v2 CSP
and Blob-resource interaction was inspected as a potential follow-up risk, not
proven or changed here. Do not treat empty-art smoke as proof of image decode.
Keep #199 draft until all required gates pass; do not start downstream gameplay
under the verify-#165-first instruction.

## Impact and rollback

Architecture: existing Phaser/TypeScript/Vite/DOM contract; no dependencies added.
Learner data/migration: no storage, schema, import, mastery or curriculum changes.
Accessibility/localisation: no UI or child-facing copy changes; approved required
content remains distinct from optional decoration. Performance: repaired accounting
and runtime approval gates, with unchanged numerical limits; production bundle
size remains unmeasured. Visual evidence is not applicable to this non-visual patch.

Revert only this continuation to return to `26c3550`; preserve PR #199's earlier
loader fixes. No main, v1 service-worker, production route, deployment, cloud,
purchase or participant-contact changes. #178 still requires explicit GO before
#179–#183; #186 still requires real authorized participants; #187 remains an
exact-candidate handoff without production authorization.
