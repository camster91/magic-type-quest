# #165 page-return repair — 25 September 2026

Status: **IMPLEMENTED AND ISOLATED-TESTED; FULL BROWSER GATE PENDING**.
Continues `598cc85a4eae4582b7cde5c01e40f2bac6561674` in existing draft PR #199.

## Reproduced failure and repair

Executed the actual prior and revised main.ts bodies in an explicit shell/DOM
port harness, removing only their imports and supplying the real revised disposal
helper. Prior entry: cached pagehide cleared the root, and pageshow left it empty.
Revised entry: the same root remains populated across that transition and is
cleared on final exit. Both versions mount once; the repair adds no remount or
second learner initialization. This is an entry-code reproduction, not a real
browser freeze or a learner-data result.

The small binding retains its final-exit listener across persisted departures,
removes it before disposal, preserves other handlers, and is idempotent. Only the
entry import/call changes; V2Shell and persistence code are unchanged by this commit.
The design and remaining IndexedDB/cache limitations are in
[PAGE-LIFECYCLE.md](../architecture/PAGE-LIFECYCLE.md).

## Checks actually run

| Check | Result and scope |
| --- | --- |
| New page-disposal tests | 12 passed |
| Entry-code reproduction | Prior blank return reproduced; revised retention/final-exit assertions passed |
| New-suite repeatability | Five further runs, all 12 passing |
| Earlier recovery suites | All 118 passed again (95 asset/scene tests plus 23 ownership tests) |
| Strict isolated TypeScript | New binder passed with strict, noUncheckedIndexedAccess, exactOptionalPropertyTypes and DOM types |
| Syntax/whitespace | New tests/configuration syntax, source final newlines and trailing whitespace checked |

Node 22.16.0 and TypeScript 5.8.3 were available. Committed tests use Vitest;
temporary recovery copies use node:test and transpiled local TypeScript imports.
No assertion is removed. The entry harness substitutes explicit DOM/shell ports,
not the full app. These results total 130 isolated cases, not 130 Playwright cases.

## Browser coverage authored, not executed

Added `e2e/v2-page-return.e2e.js` to the existing source-mode persistence config:
1. Five injected persisted transitions preserve the same actual shell/canvas,
   partial F/J input, local learner identity, v1 fixture bytes and session count.
2. A subsequent final exit still disposes the root.
3. Native Back from the parent page returns usable v2 progress, preserves stored
   identity/session evidence, and records the actual document/cache path.

The native test attaches its observed pageshow persisted flag. A reload path is
annotated as not exercising cache admission. The injected-event test does not
claim to simulate native freezing, task scheduling or IndexedDB suspension.
These three journeys and the eight world-lifecycle journeys remain unrun here.

## Blocker, acceptance and next action

The recovery workspace still has only a source subset, no complete locked checkout.
Git/npm DNS and the previously documented managed-browser restriction prevent full
execution. No restriction was changed or bypassed. No full project typecheck,
ESLint, canonical full Vitest, curriculum/content/theme/asset inventory, actual
budget build, Playwright/axe, persistence, v1, local/cloud boundary, production build,
real performance, device or learner validation pass is claimed.

Run the complete #165 command matrix against the exact updated head. Include the
new startup source guard, page-return and world-lifecycle suites, actual native
cache/device behaviour, v1 data preservation, persistence, low-end performance and
both production builds. Resolve failures before marking PR #199 ready or merging.
#166 remains behind #165 verification; no integration or production merge is made.

## Rollback

Revert only this page-return commit to return to its parent. No data migration,
reset, schema rollback, production action or cloud change is necessary or allowed.
